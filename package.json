{
  "name": "difftrail",
  "private": true,
  "version": "0.1.3",
  "description": "Difftrail Claude Code plugin — release tooling",
  "scripts": {
    "changeset": "changeset",
    "version": "changeset version && node scripts/sync-versions.mjs",
    "test": "node --test"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0",
    "@changesets/changelog-github": "^0.5.0"
  }
}
